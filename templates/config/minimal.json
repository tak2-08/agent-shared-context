{
  "$schema": "../docs/config.schema.json",
  "version": 1,
  "project": {
    "name": "my-app",
    "displayName": "My App",
    "prefix": "ma",
    "description": "Minimal config — 10 lines"
  },
  "contextRoot": "agent-context",
  "features": {
    "api": { "label": "API", "files": ["src/api/index.ts:1"], "description": "REST" }
  },
  "graph": { "edges": [] },
  "storage": { "backend": "json", "softLimits": { "softLimitChars": 200000, "maxEntries": 1000 } }
}
